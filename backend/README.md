# Text-to-SQL Backend

FastAPI backend service for the Text-to-SQL AI Agent.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e .
```

## Run

```bash
uvicorn text_to_sql.main:app --reload
```
